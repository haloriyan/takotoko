# User Management Features.

I need you create a page that located in a sub menu `Master Data > Users`. Before we get further, i just need a standard CRUD operations and a search feature. You should start the work from `routes/admin.php` that means this started with `admin` route path prefix.

I want the user list rendered in a horizontal scrollable table. Do not add any custom CSS other than standard tailwindcss classes. Instead of rendering so much buttons for the actions, you should use a square rounded box with triple dot icons to trigger a context menu when this button hovered.